syntax = "proto3";

option java_package = "com.malikazizali.challengechapter6";
option java_multiple_files = true;

message UserProto {
  // filter for showing / hiding completed tasks
  string namaLengkap = 1;
  string username = 2;
  string password = 3;
  string session = 4;
}